interface:
  display_name: "Start"
  short_description: "Route mixed work to the right workflow"
  default_prompt: "Use $real-start to route this request to the smallest fitting workflow."
policy:
  allow_implicit_invocation: true
